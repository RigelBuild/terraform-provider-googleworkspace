name: release

# Sealed fork release. A `v*-sealed.*` tag (e.g. v0.7.0-sealed.1) builds the
# provider binary for every consumer platform and publishes a GitHub Release
# carrying the raw binaries plus an aggregated SHA256SUMS. Consumers fetch the
# pinned asset and verify its digest against a committed lockfile — no registry,
# no signing, no local Go toolchain.

on:
  push:
    tags:
      - "v*-sealed.*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - { goos: linux, goarch: amd64 }
          - { goos: linux, goarch: arm64 }
          - { goos: darwin, goarch: arm64 }
          - { goos: darwin, goarch: amd64 }
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version-file: go.mod
      - name: Build provider binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          out="terraform-provider-googleworkspace_${GITHUB_REF_NAME}_${GOOS}_${GOARCH}"
          go build -trimpath -ldflags '-s -w' -o "$out" .
          sha256sum "$out" > "$out.sha256"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: bin-${{ matrix.goos }}-${{ matrix.goarch }}
          path: terraform-provider-googleworkspace_*

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
        with:
          path: dist
          merge-multiple: true
      - name: Aggregate SHA256SUMS
        run: |
          cd dist && cat *_*.sha256 > SHA256SUMS && rm -f *.sha256
      - uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda # v2.2.1
        with:
          files: |
            dist/terraform-provider-googleworkspace_*
            dist/SHA256SUMS
          fail_on_unmatched_files: true
